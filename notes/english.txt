Both languages have almost the same syntax.
It is easy for programmers to switch to c++ or vice versa.
Our online c++ editor runs directly in your browser,and shows both the code and the result.
In c++ ,the main function is expected to return an integer value to the operating system,which
indicates the program's exit status.The correct declaration of main is int main.
Every c++ statements ends with a semicolon ; .
The complier ignores with spaces.
Both approaches are valid in c++.
As you write large program ,you may prefer using std:: explicitly.
Anosther way to insert a new line ,is with the endl manipulator.
Use int when you need to store a whole number without decimals.
When should i use one over the onther.